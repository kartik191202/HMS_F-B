export function DataTable<T extends Record<string, unknown>>({ rows, columns }: { rows: T[]; columns: (keyof T)[] }) { return <div className="overflow-x-auto rounded border bg-white"><table className="w-full text-left text-sm"><thead><tr>{columns.map((column) => <th className="border-b px-4 py-3" key={String(column)}>{String(column)}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={index}>{columns.map((column) => <td className="border-b px-4 py-3" key={String(column)}>{String(row[column] ?? "")}</td>)}</tr>)}</tbody></table></div>; }
